---
- job:
    name: Openfire
    description: Builds and deploys IMYD fork of Openfire.  This configuration managed via ansible, changes made manually will be lost.
    parameters:
      - string:
          name: Message
          default: Hello World
          description: A message to echo
    builders:
      - shell: "echo $Message"
